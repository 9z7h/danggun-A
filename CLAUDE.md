# 당근부동산 프로토타입 — CLAUDE.md

당근마켓 부동산(당근부동산) 모바일 앱의 인터랙티브 프로토타입입니다.
실제 앱 스크린샷을 기준으로 SEED Design 시스템 토큰을 적용해 제작했습니다.

## 핵심 파일 (다중 파일 구조)

- `index.html` — **진입점**. 마크업만 담고, `styles.css` + `data.js` + `app.js`를 링크.
- `styles.css` — 전체 CSS (SEED 토큰 `:root` 변수 + 화면별 스타일).
- `data.js` — **데이터 레이어**. 하드코딩 배열(`clusters` `pois` `POOL` `FILTERS` `WISH` `APPL` 등)과 아이콘 SVG 상수. API 연동 시 이 파일만 교체.
- `app.js` — **로직 레이어**. 마커 렌더, 팬/줌, 바텀시트, 필터, 상세 페이지, 관심목록 등 모든 동작.
- `design.md` — SEED Design 시스템 토큰/컴포넌트 레퍼런스 (색·타이포·spacing·radius·Do/Don't).
  디자인 결정을 내릴 때 이 파일의 role 토큰을 우선 참고할 것.
- `realty-prototype.html` — (레거시) 분리 전 단일 파일. 참고용으로만 남겨둠. 수정은 위 4개 파일에서.

> 로드 순서: `data.js` → `app.js`. 둘 다 전역 `var`로 스코프를 공유하므로 `data.js`의 배열을 `app.js`가 그대로 참조합니다. 빌드 도구·모듈 시스템 없음(의도적).

## 실행 / 미리보기

```bash
open "index.html"        # macOS
# 또는 브라우저에 드래그앤드롭
```

`file://`로 바로 열려도 동작합니다(외부 네트워크 의존성 0).

모바일 프레임은 **375 × 812** 고정 비율. 레이아웃 구조(각 요소 px 좌표)는 항상 이 기준.
- 데스크톱: 뷰포트 안에 비율 유지(contain)로 축소 표시 — 폰 목업처럼 여백(회색 배경)이 보임.
- 실제 모바일/터치 기기(`@media (hover:none) and (pointer:coarse)`, [styles.css](styles.css) `body` 규칙): 비율 유지 채움(cover)으로 확대 — 화면을 여백 없이 꽉 채우되, 기기 종횡비가 375:812와 다르면 상/하 또는 좌/우가 살짝 잘릴 수 있음.
터치 제스처는 모바일/터치 디바이스에서, 데스크톱은 마우스 드래그 + 휠 + 더블클릭으로 대체됩니다.

## 화면 구조 (z-index 레이어)

모든 화면은 `.screen`(375×812) 안에 절대배치되며 z-index로 쌓입니다.

| 레이어 | z-index | 설명 |
|---|---|---|
| 지도 (map-vp / world) | 0 | 팬·줌되는 베이스 지도 + 마커 |
| 지도 UI 오버레이 | 25~30 | 검색바, 필터칩, 매물/단지/중개 세그먼트, 현위치, 하단 필 |
| 바텀시트 (overlay/sheet) | 40~41 | 매물 리스트 / 필터 옵션 시트 |
| 관심목록 (wish) | 70 | 찜한 매물 풀스크린 |
| 매물 상세 (detail) | 80 | 매물 상세 풀스크린 |
| 스낵바 (snackbar) | 90 | 찜 추가/삭제 피드백 |

> 레이어 순서 규칙: 상세(80)는 관심목록(70) 위에 떠야 "관심목록 → 매물 → 뒤로 → 관심목록" 흐름이 성립.
> 스낵바(90)는 상세의 찜 하트에서 호출되므로 상세보다 위여야 함. 새 풀스크린/모달을 추가하면 이 표를 갱신할 것.

## 주요 인터랙션 (모두 Vanilla JS, `app.js`)

- **지도 팬/줌** — Pointer 이벤트로 드래그 팬, 두 손가락 핀치 줌, 휠/더블클릭 줌. `world` 컨테이너에 `transform: translate() scale()` 적용, 경계 clamp.
- **탭 vs 드래그 구분** — pointerup 시 이동거리<8px & 350ms 이내면 탭으로 간주, `elementFromPoint`로 클러스터/매물 핀 판정.
- **클러스터 탭** → 줌인 + "매물 N개" 리스트 바텀시트.
- **필터칩** → 옵션 바텀시트 (단일/다중 선택, 칩 라벨 갱신). 설정은 `FILTERS` 객체.
- **매물 리스트 카드** → 매물 상세 페이지(`openDetail`).
- **매물 상세** → 스티키 탭바(매물설명·시설정보·위치·상세정보·살아본후기·중개소후기) + 스크롤 스파이. 탭 클릭 시 세로(섹션 점프)와 가로(탭바 센터링)를 분리 처리.
- **찜 하트(리스트 카드 / 상세 CTA)** → 즉시 `WISH`에 추가 후 스낵바("관심목록에 추가했어요 · 바로가기", 4초 유지). 바로가기 탭 시 관심목록 페이지로 이동.
- **관심목록 보기** → 관심목록 페이지(`openWishlist`). 하트 취소 시 리스트에서 제거, 카드 탭 시 상세 열림.

## 데이터 (`data.js` 배열로 하드코딩 — API 연동 시 여기만 교체)

- `clusters` — 지도 클러스터 마커 `{x, y, v}` (world 좌표, 매물 수).
- `pois` — 지도 POI 라벨 `{x, y, t, k}` (건물/학교/시장/공원/관공서).
- `POOL` — 매물 리스트/상세용 매물 `{d 거래유형, p 가격, k 종류, py 평, m2, fl 층, mg 관리비, dist 역거리, dong, like, chat, best 우수중개소, imgs 실사진 경로배열}`. `imgs`는 20개 매물 전부에 존재(매물당 2~3장, `image/N-n.png`). `imgs`가 비어 있으면 `dtPhotoSVG()` SVG로 폴백.
- `WISH` — 관심목록 매물 `{ph 사진타입, t 제목, loc, tm 시간, p 가격, chat, like}`.
- `FILTERS` — 필터칩 옵션 정의.
- `APPL` — 상세 시설정보 가전 목록 `[이름, 보유여부, svg path]`.

## 디자인 컨벤션 (design.md 기반)

- 색은 raw 팔레트가 아니라 **역할 토큰**으로. CSS `:root` 변수 사용 (`--carrot`, `--ink`, `--ink-2/3`, `--cluster`, `--line2` 등).
- 브랜드 컬러는 Carrot 오렌지(`#FF6F0F`). 보라색을 브랜드 CTA로 쓰지 말 것.
- pill/배지/세그먼트는 `border-radius: 9999px`(`--full`), 카드/시트는 12~16px.
- 폰트 스택은 Pretendard 우선, 그 뒤 SEED 시스템 sans 스택.
- 라벨·버튼 텍스트는 sentence case, 한국어 UI 문구는 실제 당근 톤 유지.

## 제약 / 알려진 한계

- **지도 타일 없음** — 실제 지도 SDK(네이버/카카오 맵) 대신 SVG로 도로·공원·2호선을 양식화. 운영 전환 시 지도 SDK 위에 마커 레이어만 얹는 구조로 교체.
- **상세 캐러셀은 실사진** — 매물 상세 캐러셀은 20개 매물 모두 `image/` 폴더의 실제 PNG 사진(`POOL[i].imgs`, 매물당 2~3장)을 띄움(`imgs` 비면 `dtPhotoSVG()` 폴백). 단, 리스트 카드/관심목록 썸네일은 여전히 인라인 SVG 일러스트(`roomSVG()`, `wPhoto()`(app.js)) — 실제 이미지 URL을 넣으면 동일하게 교체 가능.
- 빌드 도구·모듈 시스템 없음 — `app.js`가 전역 함수/변수 하나의 스코프. 규모가 더 커지면 ES 모듈로 분리 고려.

## 완료된 작업

- 매물 상세 사진 영역을 좌우 스와이프 캐러셀로 구현. (포인터 드래그 + 스냅, 닷/카운터 인디케이터) 20개 매물 전부 `POOL[i].imgs`의 실사진 PNG(`image/` 폴더, 매물당 2~3장)를 띄움. 슬라이드 수는 `PHOTO_N = imgs.length`로 자동 조정([app.js:480-481](app.js#L480-L481)). `imgs`가 없는 경우만 `dtPhotoSVG()` placeholder로 폴백.
- 풀스크린 페이지(상세·관심목록) 전환을 옆에서 미는 슬라이드 → **페이드 + 진입 스켈레톤 UI 0.5초**로 교체(실제 당근 톤). `openPage()`(app.js)가 `.loading` 클래스를 0.5s 부여, `.skel` 오버레이(styles.css, shimmer 애니메이션)를 띄운 뒤 콘텐츠 페이드인. 스켈레톤 마크업은 `#dtSkel`/`#wishSkel`에 app.js에서 주입.

## 더 키울 때

- 화면별(map / sheet / detail / wishlist)로 `app.js`를 ES 모듈 분리,
- `data.js`를 JSON + fetch로 교체,
- 컴포넌트 단위(예: 매물 카드)를 템플릿 함수로 모듈화.
